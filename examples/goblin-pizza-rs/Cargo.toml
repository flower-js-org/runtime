[package]
name = "goblin-pizza"
version = "0.1.0"
edition = "2024"
rust-version = "1.96"
description = "The Goblin Pizza benchmark application as a Rust guest module"
license = "MIT"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
flower-sdk = { path = "../../crates/flower-sdk" }
