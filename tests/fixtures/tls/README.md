Public test fixture only. The private key in this directory has no production use.
The server certificate is signed by the adjacent test CA and permits only DNS localhost,
so IP-name mismatch tests must fail verification. Generated 2026-09-24; valid for ten years.
The CA private key was discarded. Regenerate with openssl req/x509 when these fixtures expire.
